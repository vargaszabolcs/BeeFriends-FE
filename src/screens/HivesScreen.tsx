import { StyleSheet, Text, View } from "react-native";
import React from "react";

const HivesScreen = () => {
    return (
        <View>
            <Text>HivesScreen</Text>
        </View>
    );
};

const styles = StyleSheet.create({});

export default HivesScreen;
